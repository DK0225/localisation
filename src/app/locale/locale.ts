export interface Locale {
    currencyCode: string,
    app: {
      header: string,
      greeting: string,
      example: string
    },
    home: {
      header: string,
      example: string,
      list: string
    }
  }